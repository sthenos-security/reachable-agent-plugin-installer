# reachable: doctor

Purpose: verify REACHABLE setup for the current Cursor workspace.

Doctor should report runtime, workspace wiring, MCP state, remediation state,
and next required action.

Use `reachable: doctor --configure` for local configuration that requires human
review.
