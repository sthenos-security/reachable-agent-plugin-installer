# REACHABLE For Claude Code

This folder contains Claude Code-facing metadata and bootstrap instructions.

It is a framework package only. It does not bundle the REACHABLE runtime or MCP
server.

After installing the plugin, open the target workspace and run:

```text
reachable: setup
reachable: doctor
```
