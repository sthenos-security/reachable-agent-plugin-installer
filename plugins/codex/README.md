# REACHABLE For Codex

This folder contains the Codex-facing plugin metadata and bootstrap skill.

It does not bundle the REACHABLE runtime or MCP server. After installation, run:

```text
reachable: setup
reachable: doctor
```

The local MCP server named `reachable` is configured after setup when the
runtime is installed or verified.
