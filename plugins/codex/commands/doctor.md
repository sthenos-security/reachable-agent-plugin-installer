# reachable: doctor

Purpose: verify REACHABLE setup for the current Codex workspace.

Doctor should report:

- runtime installed or missing;
- workspace wired or not wired;
- MCP configured or not configured;
- remediation enabled or disabled;
- next required action.

Use `reachable: doctor --configure` for local configuration that requires human
review.
